/**
 * نامهٔ پیوستِ جدول کمیسیون — از صدای کارشناس تا فایل Word روی سربرگ
 *
 * چرا صدا: کارشناس اغلب در کارگاه و با گوشی کار می‌کند. توضیح دادنِ اینکه چه
 * اتفاقی افتاده با حرف زدن یک دقیقه طول می‌کشد و با تایپ ده دقیقه.
 *
 * مسیر: پیام صوتی تلگرام → انبار فایل → ElevenLabs (فارسی) → متن خام →
 * مدل زبانی → نامهٔ رسمی ساختاریافته → قالب سربرگ → فایل .docx
 *
 * هیچ‌کدام از این مراحل بایت‌های صوت را در Worker نگه نمی‌دارند: ElevenLabs
 * خودش فایل را از یک لینک امضاشده برمی‌دارد.
 */
import { ExtractError, MODEL } from "./extract.js";

const STT_URL = "https://api.elevenlabs.io/v1/speech-to-text";
/* ANTHROPIC_API_BASE فقط در توسعهٔ محلی ست می‌شود (مدل بدلی)؛ همان قاعدهٔ discovery.js */
const AI_URL = (env) => (env.ANTHROPIC_API_BASE || "https://api.anthropic.com") + "/v1/messages";

export const LETTER_PROMPT_VERSION = "letter/3.0";

/* مخاطب، سلام، موضوع، «با تشکر» و امضا را سامانه می‌گذارد، نه مدل (تصمیم مدیر):
   مخاطب همیشه همین است و موضوع «گزارش خرید» + عنوانِ اقلامی که کارشناس بعد از
   توضیحاتش انتخاب می‌کند، با «و» میانشان، و بعدش «، درخواست شماره …» — همان
   الگوی نامه‌های واحد. «با تشکر» و نام کارشناس هم ته نامه، سمت چپ. */
export const LETTER_TO = "مدیر محترم کمیسیون معاملات، جناب دکتر صفری";
export const LETTER_SALUTATION = "با سلام و احترام؛";
export const LETTER_THANKS = "با تشکر";
export const letterSubject = (titles, requestId) => {
  const list = [...new Set((titles || []).map((t) => String(t == null ? "" : t).trim()).filter(Boolean))];
  const base = `گزارش خرید ${list.join(" و ")}`.trim();
  const rid = String(requestId == null ? "" : requestId).trim();
  return rid ? `${base}، درخواست شماره ${rid}` : base;
};

/**
 * صوت را به متن فارسی تبدیل می‌کند.
 * `language_code: "fa"` صریح داده می‌شود چون تشخیص خودکار روی جمله‌های کوتاه
 * فارسی گاهی عربی یا اردو حدس می‌زند و کل رونویسی را خراب می‌کند.
 */
export async function transcribe(env, fileUrl) {
  if (!env.ELEVENLABS_API_KEY) throw new ExtractError("کلید سرویس تبدیل صوت ست نشده است.", 503);
  const form = new FormData();
  form.append("model_id", env.STT_MODEL || "scribe_v2");
  form.append("language_code", "fa");
  form.append("source_url", fileUrl);

  const r = await fetch(STT_URL, { method: "POST", headers: { "xi-api-key": env.ELEVENLABS_API_KEY }, body: form });
  const d = await r.json().catch(() => ({}));
  if (!r.ok) {
    const msg = (d && (d.detail?.message || d.detail || d.message)) || `خطای ${r.status}`;
    throw new ExtractError(`تبدیل صوت به متن نشد: ${String(typeof msg === "string" ? msg : JSON.stringify(msg)).slice(0, 200)}`, r.status === 429 ? 429 : 502);
  }
  return { text: String(d.text || "").trim(), language: d.language_code, confidence: d.language_probability };
}

/* ------------------------------------------------------------------ */
/* نگارش نامه                                                          */
/* ------------------------------------------------------------------ */

const LETTER_SCHEMA = {
  type: "object",
  properties: {
    paragraphs: { type: "array", items: { type: "string" }, description: "بدنهٔ نامه، هر بند یک عضو آرایه. یک تا سه بند؛ بند اول با «احتراماً به استحضار می‌رساند» شروع می‌شود." },
    closing: { type: "string", description: "جملهٔ پایانی به یکی از الگوهای واحد (درخواست موافقت / مراتب جهت استحضار …) — بدون «با تشکر»" },
    uncertain: { type: "array", items: { type: "string" }, description: "چیزهایی که در صوت مبهم بود و در نامه نیاوردی" },
  },
  required: ["paragraphs", "closing"],
  additionalProperties: false,
};

/*
 * سبک از نُه نامهٔ واقعی واحد تدارکات و پشتیبانی برداشته شده (شهریور ۱۴۰۵؛ نامه‌های
 * ۲۱۰۲۸۶۸، ۲۱۰۲۹۱۴، ۳۱۰۱۲۰۲، ۳۱۰۱۳۴۸، ۳۱۰۱۳۶۰، ۳۱۰۱۳۶۹، ۳۱۰۱۴۹۰، ۴۱۱۰۴۵۴، ۴۱۱۰۴۸۹).
 * الگوی مشترکشان: مخاطب ← «موضوع: خرید …، درخواست شماره …» ← «باسلام؛» ← یک تا سه بندِ
 * روایی که با «احتراماً به استحضار می‌رساند» شروع می‌شود و ماجرای خرید را به ترتیبِ رخداد
 * می‌گوید ← جملهٔ پایانیِ درخواست/ارائه ← «باتشکر» و نام کارشناس، سمت چپ.
 * مخاطب، سلام، موضوع، «با تشکر» و امضا را کد می‌گذارد؛ مدل فقط بدنه و جملهٔ پایانی را می‌نویسد.
 */
const LETTER_SYSTEM = `تو نامه‌های اداری واحد تدارکات و پشتیبانی یک شرکت پیمانکاری ایرانی را می‌نویسی — نامه‌ای که کارشناس خرید
پیوستِ جدول کمیسیون می‌کند و مدیر کمیسیون معاملات می‌خواند.

ورودی تو دو چیز است: (۱) رونویسیِ صحبت شفاهی (یا نوشتهٔ) کارشناس خرید که توضیح می‌دهد در جریان این درخواست خرید
چه گذشته، و (۲) اطلاعات همان درخواست و استعلام‌ها. خروجی تو فقط بندهای بدنه (paragraphs) و جملهٔ پایانی (closing) است؛
مخاطب، سلام، موضوع، «با تشکر» و امضا را سامانه خودش می‌گذارد.

سبک نامه‌های این واحد — از نامه‌های واقعی واحد برداشته شده؛ عیناً همین سبک را بنویس:

• کوتاه: یک تا سه بند. بیشتر نامه‌های واحد یک بندِ پیوستهٔ چندجمله‌ای دارند؛ بند دوم و سوم فقط وقتی ماجرا
  مرحلهٔ جدایی دارد (مثلاً بررسی فنی، یا مذاکرهٔ بعدی). هر بند یک مرحله از ماجرا.
• بند اول همیشه با «احتراماً به استحضار می‌رساند» یا «احتراماً بدین‌وسیله به استحضار می‌رساند» شروع می‌شود و
  بلافاصله وارد ماجرا می‌شود؛ مقدمه‌چینی و تکرارِ موضوع نامه ندارد.
• روایتِ واقعی و به ترتیبِ رخداد: نمونه یا درخواست به کدام فروشگاه‌ها و تأمین‌کنندگان رفت، چه شد (موجود نبود،
  کیفیت مناسب نبود، سایز متفاوت بود، قیمت پایین‌تر بود، قطعه قابل تعمیر بود)، با چه کسی هماهنگ شد، چه تخفیفی
  گرفته شد، چرا این تأمین‌کننده انتخاب شد. فقط آنچه کارشناس گفته یا در داده هست.
• فعل‌های مجهول و اداری، همان‌هایی که واحد می‌نویسد: «ارسال گردید»، «استعلام گرفته شد»، «اخذ شد»، «اخذ گردید»،
  «مقرر شد»، «مشخص شد»، «پیشنهاد نشد»، «جایگزین شد»، «مورد تأیید قرار گرفت»، «شناسایی و در رتبهٔ اول
  تأمین‌کنندگان قرار گرفت»، «موفق به اخذ … درصد تخفیف نسبت به قیمت اولیه شدیم»، «به کارگاه ارائه گردید».
• پیوندهای رایج میان جمله‌ها: «سپس»، «در ادامه»، «همچنین»، «لذا»، «با توجه به»، «به همین دلیل»، «در نهایت»،
  «لازم به ذکر است».
• اشخاص و واحدها فقط اگر کارشناس نام برده، به همان شکل اداری: «با هماهنگی واحد ماشین‌آلات، جناب آقای …»،
  «با تأیید جناب آقای مهندس … مدیرعامل محترم»، «بر اساس نظر مشاور پروژه»، «با هماهنگی جنابعالی».
• جملهٔ پایانی (closing) یکی از الگوهای واحد است، متناسب با ماجرا:
    «لذا با توجه به …، درخواست می‌شود خرید … از … به صورت تک‌استعلام مورد موافقت قرار گیرد.»
    «خواهشمند است در صورت تأیید کمیسیون مذکور، دستور فرمایید … .»
    «مراتب جهت استحضار و اتخاذ تصمیم مقتضی به حضور کمیسیون محترم ارائه می‌گردد.»
    «پیشنهاد می‌شود در جلسهٔ کمیسیون معاملات، با تأمین‌کنندگان منتخب در خصوص قیمت نهایی مذاکره صورت پذیرد.»
    «لازم به ذکر است مدارک مربوطه پیوست گردیده است.» ← فقط اگر کارشناس از مدرک یا پیوست گفته.
• لحن توضیحی است، نه دفاعی، نه تبلیغی؛ بی اغراق و بی صفتِ اضافه. کمیسیون باید بفهمد چه شد و چرا.

نمونهٔ سبک (بندهایی از نامه‌های واحد؛ نام‌ها و عددها برداشته شده):
«احتراماً بدین‌وسیله به استحضار می‌رساند نمونه جهت بررسی به فروشگاه‌های … و … ارسال شد، اما به دلیل عدم موجودی به
فروشگاه … ارسال شد که نمونهٔ چینی موجود داشت، ولی به دلیل اینکه کیفیت و عملکرد مناسبی برای این کار نداشت، پیشنهاد نشد.
در ادامه، قطعات برای تعمیرگاه …، آقای …، ارسال شد که قطعات مورد نیاز را موجود داشتند و پس از بررسی مشخص شد قطعات
موجود قابل تعمیر و استفاده هستند؛ به همین دلیل قطعهٔ استوک سالم جایگزین شد و هزینهٔ انجام کار کاهش پیدا کرد.»
«احتراماً به استحضار می‌رساند با توجه به درخواست کارگاه … مبنی بر خرید … برند … و اعلام مشخصات واردکنندهٔ مربوطه،
اقدام به اخذ استعلامات لازم شد و طی بررسی‌های انجام‌شده، شرکت معرفی‌شده به عنوان واردکنندهٔ انحصاری این برند شناسایی و
در رتبهٔ اول تأمین‌کنندگان قرار گرفت. پس از انجام مذاکرات با تأمین‌کنندهٔ مذکور، موفق به اخذ … درصد تخفیف نسبت به
قیمت اولیه شدیم.»
«احتراماً به استحضار می‌رساند در راستای تأمین … مورد نیاز پروژه، ابتدا استعلام قیمت از … اخذ و به سبب قیمت مناسب‌تر
به کارگاه ارائه گردید؛ پس از بررسی فنی توسط کارگاه مشخص شد محصول مذکور با مشخصات مورد نیاز مغایرت دارد. بر اساس نظر
مشاور پروژه، خرید این محصول از برند … مورد تأیید قرار گرفت. لذا با توجه به الزامات فنی پروژه و تأیید مشاور، درخواست
می‌شود خرید … از شرکت … به صورت تک‌استعلام مورد موافقت قرار گیرد.»

قواعد:

۱. فقط از چیزی بنویس که در صوت یا در داده‌های درخواست آمده. هیچ دلیل، عدد، تاریخ یا اسمی از خودت اضافه نکن.
   اگر کارشناس چیزی را مبهم گفته، در uncertain بنویسش و در متن نامه نیاور.

۲. حرف شفاهی را به نثر اداری برگردان، نه اینکه عیناً نقل کنی. «طرف جواب نداد» می‌شود
   «تأمین‌کنندهٔ مذکور به استعلام ارسالی پاسخ نداد». ولی معنا را عوض نکن و چیزی به آن نیفزا.

۳. اگر کارشناس از قیمت یا تأمین‌کننده‌ای حرف زده که در داده‌های استعلام هست، همان نام رسمی را
   به کار ببر (و برای عدد، جای‌خالیِ قاعدهٔ ۸)، نه تقریبِ شفاهی را.

۴. اگر رونویسی آن‌قدر کوتاه یا نامفهوم است که نمی‌شود نامه‌ای از آن ساخت، در paragraphs یک بند بنویس که
   توضیح کارشناس ناقص بوده و در uncertain دلیلش را بیاور. نامهٔ توخالی نساز.

۵. جمعِ دو تأمین‌کننده را وقتی برای تعداد اقلام متفاوتی قیمت داده‌اند مقایسه نکن.
   اگر یکی برای دو قلم قیمت داده و دیگری فقط برای یک قلم، جمع کلشان قابل مقایسه نیست و
   نوشتنِ «الف گران‌تر از ب بود» غلط و گمراه‌کننده است. در چنین حالتی یا قیمتِ همان قلمِ مشترک را
   مقایسه کن، یا اصلاً مقایسهٔ عددی نکن و فقط بنویس کدام تأمین‌کننده برای کدام اقلام قیمت داده.
   در متن ورودی، تأمین‌کننده‌ای که همهٔ اقلام را قیمت نداده صریح علامت خورده است.

۶. درخواستِ اقدام فقط یک بار و فقط در closing بیاید؛ بدنه شرحِ ماجراست، نه تقاضا. closing با «با تشکر» تمام نمی‌شود
   و سلام، مخاطب، موضوع، تاریخ و شمارهٔ نامه هیچ‌جا نوشته نمی‌شود — سامانه می‌گذاردشان.

۷. تاریخ شمسی را همیشه سال/ماه/روز بنویس: «۱۴۰۵/۰۶/۱۸»، نه «۱۸/۰۶/۱۴۰۵». در متن راست‌به‌چپ،
   عددها از چپ خوانده می‌شوند و ترتیبِ وارونه، تاریخ را وارونه نشان می‌دهد.
   کلمه‌های مرکب را هم با نیم‌فاصله بنویس: «تأمین‌کننده»، «پیش‌فاکتور»، «قیمت‌ها»، «می‌شود».

۸. **هیچ عدد مالی یا شمارشی را خودت ننویس.** جمع مبلغ، قیمت، تعداد اقلام، شمارهٔ درخواست و تاریخ درخواست
   را با جای‌خالی بگذار؛ سامانه از دادهٔ خودش پرشان می‌کند. جای‌خالی‌های مجاز (دقیقاً همین شکل، با همان
   نام تأمین‌کننده و قلمی که در «اطلاعات درخواست» آمده):
     {{شماره_درخواست}}  {{تاریخ_درخواست}}  {{طرف_مقابل}}
     {{تعداد_اقلام_کمیسیون}}   ← چند قلم در جدول کمیسیون است
     {{تعداد_اقلام_درخواست}}   ← کل اقلام درخواست
     {{اقلام_کمیسیون}}          ← فهرست عنوان اقلام جدول، با ویرگول
     {{جمع|نام تأمین‌کننده}}     ← جمع مبلغ همان تأمین‌کننده برای اقلام جدول، به ریال
     {{تعداد|نام تأمین‌کننده}}   ← برای چند قلم قیمت داده
     {{قیمت|نام تأمین‌کننده|عنوان قلم}} ← قیمت واحد یک قلم
   مثال: «جمع مبلغ پیشنهادی {{جمع|شرکت آریا}} ریال برای {{تعداد|شرکت آریا}} قلم».
   عددی که در «حرف کارشناس» آمده و در داده نیست (مثلاً «سه بار زنگ زدم»، «سه درصد تخفیف») را با حروف بنویس.`;

/**
 * نامه را از رونویسی صوت و بافتِ درخواست می‌سازد.
 * خروجی ساختاریافته است تا قالب Word بدون تجزیهٔ متن پرش کند.
 */
/**
 * دادهٔ نامه از دادهٔ خام ارجاع: فقط استعلام‌های **تیک‌خورده** و قلم‌هایی که همان‌ها
 * قیمت داده‌اند. همان قاعدهٔ جدول کمیسیون — نامه پیوستِ همان جدول است.
 */
export function letterData({ request, items, quotes, allItems }) {
  const finals = (quotes || []).filter((q) => q.final && q.saved);
  const covered = new Set(finals.map((q) => q.item_id));
  const rows = (items || []).filter((i) => covered.has(i.id));
  const suppliers = [...new Set(finals.map((q) => q.supplier_name))].map((name) => {
    const qs = finals.filter((q) => q.supplier_name === name);
    return {
      name,
      count: qs.length,
      sum: qs.reduce((n, q) => n + (+q.price || 0) * (+q.qty || 0), 0),
      prices: qs.map((q) => ({ item: (rows.find((i) => i.id === q.item_id) || {}).title || "", price: +q.price || 0 })),
    };
  });
  return {
    request, items: rows, suppliers,
    totalItems: (allItems || items || []).length,
  };
}

const money = (n) => Number(n || 0).toLocaleString("en-US");
const norm = (s) => String(s == null ? "" : s).replace(/[ي]/g, "ی").replace(/[ك]/g, "ک").replace(/[\u200c\s]+/g, " ").trim().toLowerCase();
const same = (a, b) => { const x = norm(a), y = norm(b); return x === y || (x.length > 3 && y.includes(x)) || (y.length > 3 && x.includes(y)); };

/**
 * جای‌خالی‌های نامه را از دادهٔ سامانه پر می‌کند.
 *
 * چرا این‌جا و نه در مدل: مدل یک بار «هفت قلم» و جمعِ هفت قلم را نوشت وقتی فقط
 * دو قلم تیک خورده بود. عددی که در نامه می‌نشیند باید همان عددِ جدول کمیسیون
 * باشد، و آن را فقط کد می‌تواند تضمین کند. هر جای‌خالیِ حل‌نشده «—» می‌شود و
 * گزارش می‌شود؛ هر عددِ بزرگی که مدل خودش نوشته و در داده نیست، مشکوک علامت
 * می‌خورد تا کارشناس پیش از فرستادن ببیند.
 */
export function fillLetter(letter, d) {
  const unresolved = new Set();
  const known = new Set([String(d.request.id), ...(d.suppliers || []).flatMap((s) => [s.sum, s.count, ...s.prices.map((p) => p.price)]).map(String)]);
  known.add(String(d.items.length)); known.add(String(d.totalItems));

  const findSup = (name) => (d.suppliers || []).find((s) => same(s.name, name));
  const resolve = (token) => {
    const parts = token.split("|").map((x) => x.trim());
    const key = parts[0];
    if (key === "شماره_درخواست") return String(d.request.id || "");
    if (key === "تاریخ_درخواست") return String(d.request.date || "");
    if (key === "طرف_مقابل") return String(d.request.party || "");
    if (key === "تعداد_اقلام_کمیسیون") return String(d.items.length);
    if (key === "تعداد_اقلام_درخواست") return String(d.totalItems);
    if (key === "اقلام_کمیسیون") return d.items.map((i) => i.title).join("، ");
    if (key === "جمع" || key === "تعداد" || key === "قیمت") {
      const s = findSup(parts[1] || "");
      if (!s) return null;
      if (key === "جمع") return money(s.sum);
      if (key === "تعداد") return String(s.count);
      const p = s.prices.find((x) => same(x.item, parts[2] || ""));
      return p ? money(p.price) : null;
    }
    return null;
  };
  const fill = (text) => String(text == null ? "" : text).replace(/\{\{([^{}]+)\}\}/g, (m, tok) => {
    const v = resolve(tok);
    if (v == null) { unresolved.add(tok.trim()); return "—"; }
    return v;
  });

  const out = { ...letter };
  for (const k of ["subject", "to", "salutation", "closing"]) if (out[k] != null) out[k] = fill(out[k]);
  out.paragraphs = (letter.paragraphs || []).map(fill);

  /* عددهای بزرگی که از داده نیامده‌اند — نامزدِ اشتباهِ مدل */
  const text = [out.subject, ...(out.paragraphs || []), out.closing].join("\n");
  const digits = (x) => x.replace(/[۰-۹]/g, (c) => "۰۱۲۳۴۵۶۷۸۹".indexOf(c)).replace(/[٠-٩]/g, (c) => "٠١٢٣٤٥٦٧٨٩".indexOf(c));
  const suspicious = [];
  /* روی متنِ اصلی جست‌وجو می‌شود تا همان شکلی که کارشناس در نامه می‌بیند گزارش شود */
  for (const m of text.matchAll(/[\d۰-۹٠-٩][\d۰-۹٠-٩,٬]{3,}/g)) {
    const raw = digits(m[0]).replace(/[,٬]/g, "");
    if (raw.length >= 4 && !known.has(raw) && !/^1[34]\d\d$/.test(raw)) suspicious.push(m[0]);
  }
  return { letter: out, unresolved: [...unresolved], suspicious: [...new Set(suspicious)] };
}

/**
 * نامه را از رونویسی صوت و بافتِ درخواست می‌سازد.
 * خروجی ساختاریافته است تا قالب Word بدون تجزیهٔ متن پرش کند.
 * مدل عدد نمی‌نویسد؛ جای‌خالی می‌گذارد و fillLetter پرش می‌کند.
 */
export async function writeLetter(env, { transcript, request, items, quotes, allItems, notes, expert, expertName, company, subjectTitles }) {
  if (!env.ANTHROPIC_API_KEY) throw new ExtractError("کلید مدل ست نشده است.", 503);

  const d = letterData({ request, items, quotes, allItems });
  const supplierLines = d.suppliers.length
    ? d.suppliers.map((s) => `- ${s.name}: برای ${s.count} قلم از ${d.items.length} قلمِ جدول قیمت داده`
      + (s.count < d.items.length ? "  ⟵ ناقص است؛ جمعش با تأمین‌کنندهٔ کامل قابل مقایسه نیست" : "")
      + `\n  جای‌خالی‌هایش: {{جمع|${s.name}}} · {{تعداد|${s.name}}}`
      + s.prices.map((p) => ` · {{قیمت|${s.name}|${p.item}}}`).join("")).join("\n")
    : "هنوز استعلامِ تیک‌خورده‌ای نیست.";

  const context = `شرکت: ${company}\n`
    + `درخواست خرید {{شماره_درخواست}} مورخ {{تاریخ_درخواست}}\n`
    + `طرف مقابل / مرکز هزینه: {{طرف_مقابل}}\n`
    + `کارشناس خرید: ${expert}\n\n`
    + `این درخواست {{تعداد_اقلام_درخواست}} قلم دارد و جدول کمیسیون برای {{تعداد_اقلام_کمیسیون}} قلم زیر است:\n`
    + `${d.items.map((i) => `- ${i.title}${i.qty != null ? ` (${i.qty} ${i.unit || ""})` : ""}`).join("\n") || "—"}\n\n`
    + `تأمین‌کنندگانِ تیک‌خورده:\n${supplierLines}\n`
    + (notes ? `\nتوضیحات کارشناس در برگهٔ کمیسیون:\n${notes}\n` : "");

  const r = await fetch(AI_URL(env), {
    method: "POST",
    headers: { "content-type": "application/json", "x-api-key": env.ANTHROPIC_API_KEY, "anthropic-version": "2023-06-01" },
    body: JSON.stringify({
      model: env.AI_MODEL || MODEL,
      max_tokens: 3000,
      system: LETTER_SYSTEM,
      tools: [{ name: "write_letter", description: "نامهٔ اداری ساخته‌شده", input_schema: LETTER_SCHEMA }],
      tool_choice: { type: "tool", name: "write_letter" },
      messages: [{ role: "user", content: [{ type: "text", text:
        `<اطلاعات_درخواست>\n${context}\n</اطلاعات_درخواست>\n\n`
        + `<حرف_کارشناس>\n${transcript}\n</حرف_کارشناس>\n\n`
        + `بدنهٔ نامه و جملهٔ پایانی را به سبک نامه‌های واحد بنویس: یک تا سه بند، بند اول با «احتراماً به استحضار می‌رساند». عددها را ننویس؛ فقط جای‌خالی‌های بالا را عیناً بگذار.` }] }],
    }),
  });
  const dd = await r.json().catch(() => ({}));
  if (!r.ok) throw new ExtractError(`نگارش نامه نشد: ${String((dd.error && dd.error.message) || r.status).slice(0, 200)}`, 502);
  const use = (dd.content || []).find((c) => c.type === "tool_use");
  if (!use) throw new ExtractError("مدل نامه را ساختاریافته برنگرداند.", 502);

  const filled = fillLetter(use.input, d);
  /* موضوع: اقلامی که کارشناس انتخاب کرد؛ اگر انتخابی نیامد، همان اقلامِ جدول کمیسیون.
     امضا فقط نام کارشناس است — همان‌طور که نامه‌های واحد امضا می‌شوند. */
  const titles = subjectTitles && subjectTitles.length ? subjectTitles : d.items.map((i) => i.title);
  return {
    letter: { ...filled.letter, to: LETTER_TO, subject: letterSubject(titles, request && request.id), salutation: LETTER_SALUTATION,
      thanks: LETTER_THANKS, signature: String(expertName || expert || "").trim() },
    meta: {
      model: dd.model, prompt_version: LETTER_PROMPT_VERSION,
      tokens_in: dd.usage?.input_tokens, tokens_out: dd.usage?.output_tokens,
      unresolved: filled.unresolved, suspicious: filled.suspicious,
      items: d.items.length, suppliers: d.suppliers.length,
    },
  };
}

